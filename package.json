{
  "name": "@dreamrsi/plugin-dream-rsi",
  "version": "0.1.0",
  "description": "Dream-RSI recursive self-improvement loop for DSH: discovery-tree memory, offline replay outcome estimation (RCO), and dreaming-based policy improvement exposed as model-facing tools.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --passWithNoTests"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4",
    "yaml": "^2.6.1"
  }
}
